<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Android 13+ for showing notifications (FCM / local) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Kamera ve galeri: profil, etkinlik, ilan fotoğrafları -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Android 12 ve altı galeri için (image_picker uyumluluğu) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    
    <application
        android:label="Twenty City Runners"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link: şifre sıfırlama (tcr://reset-password → Supabase auth) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="tcr" android:host="reset-password"/>
            </intent-filter>
            <!-- Deep link: etkinlik detay (tcr:///events/eventId → /events/eventId) -->
            <!-- NOT: android:host="" zorunlu; host olmadan pathPrefix Android tarafından -->
            <!-- yok sayılır ve tüm tcr:// URI'leri eşleşir (OAuth callback çakışması). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="tcr" android:host="" android:pathPrefix="/events"/>
            </intent-filter>
            <!-- Deep link: marketplace detay (tcr:///marketplace/listingId → /marketplace/listingId) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="tcr" android:host="" android:pathPrefix="/marketplace"/>
            </intent-filter>
            <!-- Android App Links: rivlus.com & www.rivlus.com /e/* ve /m/* → doğrudan uygulamada aç -->
            <!-- NOT: rivlus.com → www.rivlus.com redirect yapıyor (Cloudflare); -->
            <!-- Android App Links redirect'i kabul etmez, bu yüzden www da gerekli. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="rivlus.com" android:pathPrefix="/e/"/>
                <data android:scheme="https" android:host="www.rivlus.com" android:pathPrefix="/e/"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="rivlus.com" android:pathPrefix="/m/"/>
                <data android:scheme="https" android:host="www.rivlus.com" android:pathPrefix="/m/"/>
            </intent-filter>
            <!-- Deep link'leri app_links paketi ile kendimiz yönetiyoruz (splash → auth → navigate) -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="false" />
        </activity>
        <!-- OAuth callback: Chrome Custom Tab'dan gelen tcr://redirect callback'ini yakalar,
             sonucu flutter_web_auth_2 plugin'ine iletir ve ana ekranı öne getirir.
             Paketin kendi CallbackActivity'si Custom Tab'ı kapatamıyordu. -->
        <activity
            android:name=".OAuthCallbackActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="tcr" android:host="redirect"/>
            </intent-filter>
        </activity>
        <!-- Mapbox token build zamanında verilir (CI/env); repo'da secret tutulmaz -->
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
